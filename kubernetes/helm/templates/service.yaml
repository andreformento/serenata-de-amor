apiVersion: v1
kind: Service
metadata:
  name: {{ include "jarbas.fullname" . }}
  labels:
    {{- include "jarbas.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jarbas.selectorLabels" . | nindent 4 }}
